import styled from 'styled-components';

const List = styled.div`
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px 16px;
  width: 100%;
  margin-top: 60px;
`;

export default List;